// Shared fee amounts (cents). Processing applies to every checkout (onboarding + shop);
// consultation is onboarding first-time only.
export const PROCESSING_FEE_CENTS = 500;
export const CONSULTATION_FEE_CENTS = 3500;
